language: node_js
node_js: "8"

os:
  - linux
  - osx

sudo: false
dist: trusty

git:
  depth: 10

matrix:
  fast_finish: true

env:
  matrix:
    - ATOM_CHANNEL=stable
    - ATOM_CHANNEL=beta

script: 'curl -s https://raw.githubusercontent.com/atom/ci/master/build-package.sh | sh'
